import React from "react";


const Dash = (props) => {
    const { checkins } = props;

    return(
        <div>
            <h1> Dash</h1>
        </div>
    )
}

Dash.propTypes = {
    checkins: PropTypes.array.isRequired
  };
  
export default Dash;
export default Dash;
export default Dash;